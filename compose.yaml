name: numeromancy

services:
  model:
    build:
      context: .
      args:
        - USER_ID=${MY_UID}
        - GROUP_ID=${MY_GID}
    tty: true
    user: ${MY_UID}:${MY_GID}
    volumes:
      - type: bind
        source: ./data
        target: /workspace/data
        bind:
          create_host_path: true
      - type: bind
        source: ./output
        target: /workspace/output
        bind:
          create_host_path: true
      - type: bind
        source: ./numeromancy
        target: /workspace/numeromancy
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: [gpu]
